export enum Tab {
    USERS = 'users',
    LINK_ON = 'linkOn',
    LINK_OFF = 'linkOff',
    COMMENT = 'comments',
    COOKIE = 'cookie',
    TOKEN = 'token',
    PROXY = 'proxy',
    ADMIN = 'admin'
}

export enum EAuth {
    IS_LOGIN = 'isLogin',
    RE_FRESSH_TOKEN = 'refreshToken'
}

export enum EUrl {
    USERS = 'users',
    LINK_ON = 'linkOn',
    LINK_OFF = 'linkOff',
    COMMENT = 'comments',
    COOKIE = 'cookie',
    TOKEN = 'token',
    PROXY = 'proxy',
    ADMIN = 'admin',
    LOGIN = 'login',
    LOGOUT = 'logout'
}